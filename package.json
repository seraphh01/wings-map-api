{
  "name": "backend",
  "version": "1.0.0",
  "description": "This folder will contain the Node.js (Express) backend for the WINGSMAG online shop.",
  "main": "index.js",
  "scripts": {
    "dev": "node index.js",
    "migrate": "for %f in (migrations\\*.sql) do docker cp %f wings-mag-db-1:/tmp/ && docker exec -i wings-mag-db-1 psql -U wingsmag -d wingsmag -f /tmp/%~nxf",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.857.0",
    "@vendia/serverless-express": "^4.12.6",
    "aws-sdk": "^2.1692.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "supertest": "^7.1.4"
  }
}
